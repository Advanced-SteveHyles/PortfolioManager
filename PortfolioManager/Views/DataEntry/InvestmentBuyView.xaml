<UserControl x:Class="PortfolioManager.Views.DataEntry.InvestmentBuyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PortfolioManager.Views.DataEntry"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <TextBlock>Quantity</TextBlock>
        <TextBox Text="{Binding Quantity}"></TextBox>
        <TextBlock>BuyPrice</TextBlock>
        <TextBox Text="{Binding PurchasePrice}"></TextBox>
        <TextBlock>Value</TextBlock>
        <TextBox  Text="{Binding TransactionValue}"></TextBox>
        <TextBlock>Charges</TextBlock>
        <TextBox  Text="{Binding Charges}"></TextBox>
        <TextBlock>PurchaseDate</TextBlock>
        <Calendar  SelectedDate="{Binding PurchaseDate}"></Calendar>
        <Button></Button>
        <TextBlock>SettlementDate</TextBlock>
        <Calendar SelectedDate="{Binding SettlementDate}"></Calendar>
        <Button></Button>
        <TextBlock>RecordPrice?</TextBlock>
        <CheckBox IsChecked="{Binding RecordPrice}"></CheckBox>
        <local:SaveCancel></local:SaveCancel>
    </StackPanel>
</UserControl>
