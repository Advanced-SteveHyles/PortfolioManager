<UserControl x:Class="PortfolioManager.Views.AccountInvestmentDetails"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PortfolioManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <GroupBox Header="Investment Details"></GroupBox>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding InvestmentMapWrappers}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                                <ColumnDefinition />
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding AccountInvestmentMapId}"></TextBlock>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding InvestmentName}"></TextBlock>
                            <Button Grid.Row="1" Grid.Column="0" Command="{Binding ViewCommand}">View</Button>
                            <Button Grid.Row="1" Grid.Column="2" Command="{Binding BuyCommand}">Buy</Button>
                            <Button Grid.Row="1" Grid.Column="3" Command="{Binding SellCommand}">Sell</Button>
                            <Button Grid.Row="1" Grid.Column="4" Command="{Binding CorporateActionCommand}">Corporate Action</Button>

                            <ContentControl Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="4" Content="{Binding InvestmentTransaction}"></ContentControl>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </ScrollViewer>
        <DataGrid ItemsSource="{Binding InvestmentMaps}"></DataGrid>
    </StackPanel>
</UserControl>
